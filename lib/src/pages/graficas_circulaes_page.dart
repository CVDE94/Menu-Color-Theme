import 'package:flutter/material.dart';

class GraficasCirculaesPage extends StatelessWidget {
  const GraficasCirculaesPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('GraficasCirculaesPage'),
      ),
    );
  }
}
